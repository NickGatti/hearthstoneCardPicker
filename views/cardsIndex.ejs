<!DOCTYPE html>
<html>

	<head>
		<meta charset="utf-8">
		<title>Cards</title>
		<style media="screen">
			* {
				font-family: sans-serif;
				box-sizing: border-box;
				text-decoration: none;
			}

			.container {
				display: flex;
				flex-direction: row;
				flex-flow: wrap;
				justify-content: center;
				align-items: center;
			}

			.card {
				min-height: 200px;
				min-width: 150px;
				border: 1px solid black;
				padding: 2%;
				margin: 1%;
			}
		</style>
	</head>

	<body>
		<h1>Cards</h1>

		<form class="" action="/addCard" method="post">
			<input type="number" name="mana" placeholder="Mana"><br>
			<input type="number" name="attack" placeholder="Attack"><br>
			<input type="number" name="health" placeholder="Health"><br>
			<input type="text" name="description" placeholder="Description"><br>
			<input type="submit">
		</form>
	</body>
	<hr>
	<h3>Your current list:</h3>
	<div class="container">
		<% for(let i = 0; i < deck.length; i++) { %>

			<div class="card">
				<p>Mana:
					<%= deck[i].mana%>
				</p>
				<p>Attack:
					<%= deck[i].attack%>
				</p>
				<p>Health:
					<%= deck[i].health%>
				</p>
				<p>
					<%= deck[i].description%>
				</p>
				<a href="/cards/remove/<%= i %>">Remove from list</a>
			</div>
			<% } %>
	</div>
	<hr>
	<h2>All cards</h2>
	<div class="container">
		<% for(let i = 0; i < cards.length; i++) { %>

			<div class="card">
				<p>Mana:
					<%= cards[i].mana%>
				</p>
				<p>Attack:
					<%= cards[i].attack%>
				</p>
				<p>Health:
					<%= cards[i].health%>
				</p>
				<p>
					<%= cards[i].description%>
				</p>
				<a href="/cards/add/<%= cards[i].id %>">Add to list</a>
			</div>
			<% } %>
	</div>

</html>